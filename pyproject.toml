[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "defect-detection-pipeline"
version = "0.1.0"
description = "End-to-end manufacturing defect detection pipeline."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["src*"]
